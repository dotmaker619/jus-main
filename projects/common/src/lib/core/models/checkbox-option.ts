/** Checkbox option. Used for the dynamic generation of a form. */
export interface CheckboxOption {
  /** Checkbox label. */
  label: string;

  /** Checkbox name. */
  name: string;

  /** Checkbox value. */
  value?: boolean;
}
