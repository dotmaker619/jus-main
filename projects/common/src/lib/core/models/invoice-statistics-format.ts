/**
 * Available formats to download invoice statistics.
 */
export enum InvoiceStatisticsFormat {
  /** CSV format. */
  CSV = 'csv',
  /** XLS format. */
  XLS = 'xls',
  /** XLSX format. */
  XLSX = 'xlsx',
}
