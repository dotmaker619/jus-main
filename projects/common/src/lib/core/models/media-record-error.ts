/** Types of errors while recording media. */
export enum MediaRecordError {
  /** Native UI is not available for recording. */
  NativeUiNotAvailable = 'NativeUiNotAvailable',
  /** Record is not started. */
  RecordIsNotStarted = 'RecordIsNotStarted',
}
