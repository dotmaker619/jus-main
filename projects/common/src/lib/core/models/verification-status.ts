/**
 * Verification status list.
 */
export enum VerificationStatus {
  /** Approved. */
  Approved = 'approved',
  /** Denied. */
  Denied = 'denied',
  /** Not verified. */
  NotVerified = 'not_verified',
}
