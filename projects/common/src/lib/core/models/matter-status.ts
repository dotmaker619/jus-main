/** Matter statuses. */
export enum MatterStatus {
  /** Status Revoked. */
  Revoked = 'revoked',
  /** Status Pending. */
  Pending = 'pending',
  /** Status Completed. */
  Completed = 'completed',
  /** Status Active. */
  Active = 'active',
  /** Status Draft. */
  Draft = 'draft',
}
